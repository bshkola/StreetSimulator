#ifndef CAMERA_H
#define CAMERA_H

struct Camera {
    float x, y;                     //coordinates of camera placed on the map
    float azimuth, angle, range;    //atributes of camera
};

#endif // CAMERA_H
